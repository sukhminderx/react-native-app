import React from 'react';
import { Text, View } from 'react-native';

const ContactDetails = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>ContactDetails Screen</Text>
     </View>
  );
};

export default ContactDetails;
